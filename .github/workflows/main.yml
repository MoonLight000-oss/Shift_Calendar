name: Build APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: kivy/buildozer:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize and Configure Buildozer
        run: |
          export BUILDOZER_ALLOW_ROOT=1

          if [ ! -f buildozer.spec ]; then
            buildozer init
          fi

          sed -i 's/^#\?android.api = .*/android.api = 30/' buildozer.spec
          sed -i 's/^#\?android.minapi = .*/android.minapi = 21/' buildozer.spec
          sed -i 's/^#\?android.ndk = .*/android.ndk = 25b/' buildozer.spec
          sed -i 's/^#\?android.accept_sdk_license = .*/android.accept_sdk_license = True/' buildozer.spec

          echo "Настройки применены:"
          grep -E "android.api|android.ndk|android.minapi|android.accept_sdk_license" buildozer.spec

      - name: Build APK
        run: |
          export BUILDOZER_ALLOW_ROOT=1
          yes | buildozer -v android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: shift-calendar-apk
          path: bin/*.apk
          if-no-files-found: error
